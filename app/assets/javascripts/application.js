// This is a manifest file that'll be compiled into application.js, which will include all the files
// listed below.
//
// Any JavaScript/Coffee file within this directory, lib/assets/javascripts, vendor/assets/javascripts,
// or vendor/assets/javascripts of plugins, if any, can be referenced here using a relative path.
//
// It's not advisable to add code directly here, but if you do, it'll appear at the bottom of the
// compiled file.
//
// Read Sprockets README (https://github.com/sstephenson/sprockets#sprockets-directives) for details
// about supported directives.
//
//= require jquery
//= require jquery_ujs
//= require jquery-tablesorter
//= require_tree .

//TODO Don't know why this stuff doesn't work. Going to use backend validation instead and come back to this later.

// $('.draft_button').one('submit', function() {      $(this).find('input[type="submit"]').attr('disabled','disabled').attr('value', 'Processing...');   });

//$('.draft_button').one('click', function() {      $(this).attr('disabled','true') });

//$('.draft_button').on('click', function(){    $(this).attr('value', "Processing...") ;});

//$('.draft_button').one('click', function() {  $(this).attr('disabled','true'); $(this).attr('value', "Processing..."); return true;  });

//   $(".league_h1").attr("style", "color:red").html("Hello jQuery");

//  $('.draft_button').attr('value', "FOUND");

//  $( "form" ).submit(function( event ) {
//  event.preventDefault();
//  });


//  $('.draft_button').on('click', function(){    $(this).attr('disabled', 'true').attr('value', "Processing...") ;});

//});
